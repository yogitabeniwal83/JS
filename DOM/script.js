let firstElements = document.querySelector("p");    // 1st element
console.dir(firstElements);

let allElements = document.querySelectorAll("p");   // all element
console.dir(allElements);



let firstElement = document.querySelector(".heading-class");    // 1st element
console.dir(firstElement);

let allElement = document.querySelectorAll(".heading-class");   // all element
console.dir(allElement);


let firstEl = document.querySelector("#id");    // 1st element
console.dir(firstEl);

// querySelectorAll by id not exit   
let allEl = document.querySelectorAll("#id");   // all element
console.dir(allEl);


// let heading = document.getElementById("heading");

// console.dir(heading);


// let headings = document.getElementsByClassName("heading-class");
// console.log(headings);
// console.dir(headings);